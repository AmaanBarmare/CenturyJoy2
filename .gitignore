# Dependencies
node_modules/
**/node_modules/

# Build output
dist/
**/dist/
build/

# Env & secrets — NEVER commit
.env
.env.*
.env.local
!.env.example
**/.env
**/.env.*
!**/.env.example

# Logs
*.log
npm-debug.log*

# Editor / OS
.DS_Store
.vscode/
.idea/

# Local Supabase
.supabase/

# Confidential / design inputs — NOT committed by default because the GitHub
# repo's visibility could not be verified. Delete these lines to track them
# once you've confirmed the repository is private.
century-joy-prd-architecture.md
Website Layout/
Brand Guidelines for General AIDs 26thMar25.pdf
logos/

# Marketing / email artifacts — kept on disk locally, not committed.
# (.gitignore only excludes them from git; the files stay in this folder.)
*.zip
Email-Version-*/

# Large source masters — kept on disk, NOT committed (too big for GitHub's
# 100MB limit / would bloat the repo). The optimized, web-served versions live
# in client/public/ (gallery webp + thumbs, videos) and ARE committed.
/CenturyPly2Images/
/anatham3d/
/videos/
