{
  "name": "century-joy",
  "private": true,
  "version": "0.1.0",
  "description": "Century Joy — invite-only 3D visualisation service portal (monorepo: client + server).",
  "scripts": {
    "postinstall": "npm --prefix server install && npm --prefix client install",
    "dev": "concurrently -k -n api,web -c magenta,cyan \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix server run build && npm --prefix client run build",
    "seed": "npm --prefix server run seed",
    "setup:storage": "npm --prefix server run setup:storage"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
