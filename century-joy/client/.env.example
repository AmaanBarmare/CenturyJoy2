# API base — leave blank in dev to use the Vite proxy (/api -> localhost:8080)
VITE_API_URL=

# Supabase (browser uses the ANON key for direct signed-URL uploads)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_SUPABASE_STORAGE_BUCKET=century-joy-files
